{
  "name": "physiodynamic",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "wrangler pages dev dist",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=physiodynamic"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.1.5",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@fontsource/roboto": "^5.1.0",
    "@fontsource/roboto-condensed": "^5.1.0",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
