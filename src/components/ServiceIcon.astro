---
// Import actual Heroicons from the heroicons package
import spineIcon from 'heroicons/24/outline/wrench.svg?raw';
import athleteIcon from 'heroicons/24/outline/arrow-trending-up.svg?raw';
import homeIcon from 'heroicons/24/outline/home-modern.svg?raw';
import handsIcon from 'heroicons/24/outline/hand-thumb-up.svg?raw';
import needleIcon from 'heroicons/24/outline/sparkles.svg?raw';
import deviceIcon from 'heroicons/24/outline/computer-desktop.svg?raw';
import waveIcon from 'heroicons/24/outline/radio.svg?raw';
import targetIcon from 'heroicons/24/outline/trophy.svg?raw';
import movementIcon from 'heroicons/24/outline/arrow-path.svg?raw';

interface Props {
  type: string;
}

const { type } = Astro.props;

const iconMap: Record<string, string> = {
  spine: spineIcon,
  athlete: athleteIcon,
  home: homeIcon,
  hands: handsIcon,
  needle: needleIcon,
  device: deviceIcon,
  wave: waveIcon,
  target: targetIcon,
  movement: movementIcon,
};

const svgContent = iconMap[type] || iconMap.device;
---

<Fragment set:html={svgContent} />

<style>
  :global(svg) {
    width: 100%;
    height: 100%;
    stroke: currentColor;
  }
</style>
